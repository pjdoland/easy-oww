# Core dependencies
openwakeword>=0.5.0
sounddevice>=0.4.6
numpy>=1.21.0
scipy>=1.7.0
pyyaml>=6.0
requests>=2.28.0
tqdm>=4.64.0

# CLI and UI
click>=8.1.0
questionary>=1.10.0
rich>=13.0.0

# System utilities
psutil>=5.9.0

# Dataset downloads
datasets>=2.0.0
soundfile>=0.12.0
torch>=2.0.0  # Required by datasets library for audio processing
torchcodec>=0.1.0  # Required by datasets library for audio encoding

# TTS (Text-to-Speech)
piper-tts>=1.3.0

# Optional GPU support (install separately)
# onnxruntime-gpu>=1.15.0
