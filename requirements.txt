# Core dependencies
openwakeword>=0.5.0
sounddevice>=0.4.6
numpy>=1.21.0
scipy>=1.7.0
pyyaml>=6.0
requests>=2.28.0
tqdm>=4.64.0

# CLI and UI
click>=8.1.0
questionary>=1.10.0
rich>=13.0.0

# System utilities
psutil>=5.9.0

# Dataset downloads
datasets>=2.0.0
soundfile>=0.12.0
torch>=2.0.0  # Required by datasets library for audio processing
torchaudio>=2.0.0  # Audio I/O and processing with PyTorch

# Model training
torchinfo>=1.8.0  # Required for OpenWakeWord full model training
torchmetrics>=0.11.0  # Required for OpenWakeWord full model training
pronouncing>=0.2.0  # Required for adversarial text generation
Metaphone>=0.6  # Phonetic algorithm for homophone detection in adversarial validation
audiomentations>=0.30.0  # Required for audio augmentation
torch-audiomentations>=0.11.0,<0.12.0  # Pin to <0.12 to avoid breaking output_type changes (OpenWakeWord compatibility)
speechbrain>=1.0.0,<2.0.0  # Speech processing toolkit (upgrade for torchaudio 2.x compatibility)
librosa>=0.10.0  # Audio processing library
resampy>=0.4.0  # Audio resampling
mutagen>=1.45.0  # Audio metadata reading
acoustics>=0.2.0  # Acoustics and psychoacoustics library
onnx>=1.15.0  # ONNX model format
onnxscript>=0.1.0  # Required for PyTorch ONNX export

# TTS (Text-to-Speech)
piper-tts>=1.3.0
openai>=1.0.0  # OpenAI API for GPT-4o-mini-TTS (uses PCM format)

# ONNX Runtime (required for model inference)
onnxruntime>=1.15.0

# Optional GPU support (install separately)
# onnxruntime-gpu>=1.15.0
